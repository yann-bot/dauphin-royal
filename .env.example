# Sanity (lus côté serveur / build via process.env)
SANITY_PROJECT_ID=ton_project_id
SANITY_DATASET=production

# Formspree — optionnel : sans cette clé, le formulaire répond OK
# mais n'envoie pas d'email (voir src/pages/api/contact.ts)
FORMSPREE_ID=ton_formspree_id
